package org.example;

//    In this challenge, you’ll have a function that takes an array as an input and returns an array.
//            But if all goes according to plan, it’ll remove the negative numbers. This is another example of a task that’ll
//            be useful when combing through data and looking for clever ways to eliminate “bad data.
public class NumberFilter {

    public int[] negativeNumberChecker(){

        int[] numbers = {-5, 8, -12, 20, -3, 15, -7, 10, -2, 18, -9, 6, -14};
        int[] filteredArray= {numbers.length};

        for (int i = 0; i < numbers.length; i++) {
            if(numbers[i]>0){
        
            }
        }
        System.out.println(filteredArray);
        return filteredArray;
    }


}
